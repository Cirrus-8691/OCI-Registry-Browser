# Values from "emoji" webApp
# running on "http://localhost"
# with a clusted redis db.

replicaCount: 1

image:
  # ne pas utiliser https mais 
  repository: localhost:32000/oci-registry-browser # Docker image
  tag: "0.1.0"                      # Docker version

# Backend ENVIRONMENT SETTINGS
pgHost: "postgresql.easiware-postgresql-dev"
pgPort: "5432"
pgName: "k8s-registry-browser"
pgUser: "postgres"
pgPass: "postgres"
pgSslMode: "disable"

redisHost: "redis-master.emoji-redis"
redisPort: "6379"
redisDb: "1"
redisPwd: "this-is-a-sample" # use the right value

logLevel: "INF" # INF, WRN, ERR, SILENCE
baseApiPath: "/oci-browser" # Warning "/" is mandatory see livenessProbe.httpGet.path:: {{ .Values.baseApiPath }}/api/probes/liveness

jwtSecretKey: "this-is-a-sample" # use the right value
seedAdminName: "this-is-a-sample" # use the right value
seedAdminPass: "this-is-a-sample" # use the right value

# ***********************************
# * ONLY HTTP
# ***********************************
ingress:
  enabled: true
  className: "nginx"
  annotations:
  # https://github.com/kubernetes/ingress-nginx/blob/main/docs/user-guide/nginx-configuration/annotations.md
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/server-tokens: "false"

  hosts:
    - host: localhost
      paths:
        - path: /oci-browser
          pathType: Prefix
  tls: []
