apiVersion: v1
kind: Service
metadata:
  name: {{ include "oci-registry-browser.fullname" . }}
  labels:
    {{- include "oci-registry-browser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    {{- include "oci-registry-browser.selectorLabels" . | nindent 4 }}
